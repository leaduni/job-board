import type { Payload } from 'payload'

// Sample migration: no-op
// Add real data changes here (backfill, field moves, etc.)

export const up = async (_payload: Payload) => {
    // No-op sample; replace with real data changes
}

export const down = async (_payload: Payload) => {
    // No-op sample
}
